CHECKOUT

<div>
  <a href="<%= page_path(current_order.pipeline_page.chance_pipeline_page.redirect_to_page_params) %>">TO CHANCE</a>
</div>

<div>
  <a href="<%= new_pay_paypal_path %>">PAYPAL</a>
</div>

<%= form_for current_order do |f| %>
  <%= render partial: "pages/partials/page_data" %>
<% end %>